<form method="post" action="{{ path('app_film_faker_delete', {'id': film_faker.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ film_faker.id) }}">
    <button class="btn">Delete</button>
</form>
