<nav class="navbar">
    <a href="{{ path('home') }}" class="navbar-logo">
        <img src="{{ asset('images/logopopcornaddict.png') }}" alt="Logo">
    </a>
    <div class="navbar-links">
        <a href="{{ path('movie_now_playing') }}">Films</a>
        <a href="{{ path('tvshows_now_playing') }}">Séries</a>
        <a href="{{ path('app_register') }}">Inscription</a>
        {% if app.user %}
            <a href="{{ path('app_logout') }}">Déconnexion</a>
        {% else %}
            <a href="{{ path('app_login') }}">Connexion</a>
        {% endif %}
    </div>
</nav>